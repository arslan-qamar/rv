name: remote_viewer
description: View-only LAN desktop viewer
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  multicast_dns: 0.3.3

flutter:
  uses-material-design: true
